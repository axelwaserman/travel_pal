.env
__pycache__/
*.pyc
.pytest_cache/
.venv/
node_modules/
dist/
pipeline/transforms/target/
pipeline/storage/
pipeline/logs/
pipeline/.telemetry/
pipeline/transforms/logs/
pipeline/transforms/.user.yml
*.egg-info/

# Editor / IDE
.idea/
.vscode/

# Obsidian local vault state (UI layout, per-user config) — vault content is tracked, this is not
vault/.obsidian/

# Python build / coverage artifacts
.coverage
coverage.xml
.nux/
build/

# Frontend caches + Playwright artifacts
.vite/
playwright-report/
test-results/

# Per-session scratch
SESSION_STATUS.md

# Claude Code per-project state (settings, sessions); not source of truth
.claude/
.claudeignore
